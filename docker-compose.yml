services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: cesis-blustream:latest
    container_name: cesis-blustream
    env_file:
      - .env
    ports:
      - "6000:6000"
    volumes:
      - ./json:/app/json
    restart: unless-stopped
